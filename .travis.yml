language: php

services:
 - mysql
php:
  - 7.1

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - php artisan migrate

before_install:
  - mysql -e 'CREATE DATABASE travis;'

script:
  - vendor/bin/phpunit